{
  "name": "r1-riepas",
  "version": "0.1.0",
  "description": "R1 Tires — tire storage management system (Phase 1 MVP)",
  "type": "module",
  "private": true,
  "scripts": {
    "import:dry-run": "tsx tools/import/importer.ts --dry-run",
    "import:report": "tsx tools/import/importer.ts --dry-run --report data/output/reconciliation.json",
    "import:emit-seed": "tsx tools/import/importer.ts --dry-run --emit app/data/real-seed.json"
  },
  "devDependencies": {
    "tsx": "^4.23.1",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
